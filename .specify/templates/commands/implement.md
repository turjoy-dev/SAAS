---
description: Execute the implementation plan by processing and executing all tasks defined in tasks.md
---

## User Input

```text
$ARGUMENTS
```

## Outline

1. Open `specs/<feature>/tasks.md`.
2. Execute tasks sequentially or in parallel [P] where independent.
3. Update `tasks.md` checkbox markers (`[x]`) as tasks are completed and verified.
4. Verify overall system functionality against user story acceptance criteria in `spec.md`.
